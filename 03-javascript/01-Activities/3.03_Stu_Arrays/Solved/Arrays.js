// First we create an array
var animals = ["Cats", "Dogs", "Sheep", "Cows"];

// Lets log two values to the console
console.log(animals[0]);
console.log(animals[2]);

// Now lets overwrite the 3rd element in the array
animals[2] = "Horses";

// Now lets add a value to the end of our array
animals.push("Chickens");